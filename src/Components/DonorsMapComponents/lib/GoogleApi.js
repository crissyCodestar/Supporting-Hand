/*
This is a scripting compilier that builds a Google Script tag link
Describes the pieces of the API we want to load in using JS object
and letting it build the endpoint string.
*/



export function GoogleApi(){
  const apiKey = `INSERT__GOOGLE__API__KEY`
  const libraries = ['places', 'geometry']
  const v = 3.31
}
